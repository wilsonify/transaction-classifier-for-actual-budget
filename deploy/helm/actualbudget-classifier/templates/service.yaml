apiVersion: v1
kind: Service
metadata:
  name: {{ include "actualbudget-classifier.fullname" . }}
  labels:
    {{- include "actualbudget-classifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "actualbudget-classifier.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
